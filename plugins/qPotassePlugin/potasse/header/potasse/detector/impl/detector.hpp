#include<potasse/detector/detector.hpp>

namespace potasse
{
  namespace detector
  {
    //template<typename PointCloud, typename NormalCloud>
      //Detector<PointCloud, NormalCloud>::Detector() {}

    //template<typename PointCloud, typename NormalCloud>
      //typename Detector<PointCloud, NormalCloud>::NormalCloudConstPtr const
      //Detector<PointCloud, NormalCloud>::normalCloud() const
      //{
        //NormalCloudConstPtr output(normalCloudConstWPtr_.lock());
        //if (static_cast<bool>(output))
          //return output;
        //throw std::bad_weak_ptr();
      //}

    //template<typename PointCloud, typename NormalCloud>
      //typename Detector<PointCloud, NormalCloud>::PointCloudConstPtr const
      //Detector<PointCloud, NormalCloud>::pointCloud() const
      //{
        //PointCloudConstPtr output(pointCloudConstWPtr_.lock());
        //if (static_cast<bool>(output))
          //return output;
        //throw std::bad_weak_ptr();
      //}

    //template<typename PointCloud, typename NormalCloud>
      //Detector<PointCloud, NormalCloud> &
      //Detector<PointCloud, NormalCloud>::normalCloud(NormalCloudConstPtr normalCloudConstPtr)
      //{
        //normalCloudConstWPtr_ = normalCloudConstPtr;
        //return *this;
      //}

    //template<typename PointCloud, typename NormalCloud>
      //Detector<PointCloud, NormalCloud> &
      //Detector<PointCloud, NormalCloud>::pointCloud(PointCloudConstPtr pointCloudConstPtr)
      //{
        //pointCloudConstWPtr_ = pointCloudConstPtr;
        //return *this;
      //}
  }
}

