#ifndef _POTASSE_DETECTOR_PLANE_HPP_
#define _POTASSE_DETECTOR_PLANE_HPP_

#include<potasse/detector/shape.hpp>

namespace potasse
{
  namespace detector
  {
    //template<typename PointCloud, typename NormalCloud>
      //class Plane: public ShapeDetector<PointCloud, NormalCloud>
    //{
      //public:
        //typedef typename ShapeDetector<PointCloud, NormalCloud>::Parameter Parameter;

        //explicit Plane(Parameter const &parameter);

        //typedef typename ShapeDetector<PointCloud, NormalCloud>::Point Point;
        //typedef typename ShapeDetector<PointCloud, NormalCloud>::Scalar Scalar;

        //typedef typename ShapeDetector<PointCloud, NormalCloud>::PointCloudConstPtr PointCloudConstPtr;
        //typedef typename ShapeDetector<PointCloud, NormalCloud>::NormalCloudConstPtr NormalCloudConstPtr;
        //typedef typename ShapeDetector<PointCloud, NormalCloud>::Indices Indices;

        //typedef typename ShapeDetector<PointCloud, NormalCloud>::Detected Detected;
        //typedef typename ShapeDetector<PointCloud, NormalCloud>::DetectedPtr DetectedPtr;
        //typedef typename ShapeDetector<PointCloud, NormalCloud>::VertexConstPtr VertexConstPtr;

        //DetectedPtr compute
          //( PointCloudConstPtr pointCloudConstPtr,
            //NormalCloudConstPtr normalCloudConstPtr,
            //VertexConstPtr const vertexConstPtr );

      //protected:

      //private:

    //};
  }
}

#endif //_POTASSE_DETECTOR_PLANE_HPP_
